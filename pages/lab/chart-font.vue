<template>
    <div>
                <!-- Pie Chart -->
        <!-- <div class="flex justify-center">
          <Chart type="doughnut" :data="{
            labels: ['พื้นที่ตรวจสอบแล้ว', 'พื้นที่ตรวจสอบ'],
            datasets: [{
              data: [areaData.unchecked, areaData.checked],
              backgroundColor: ['#3b82f6', '#e5e7eb'],
            }]
          }" :options="{
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: {
            labels: {
              font: {
                family: 'Noto Sans Thai', // กำหนดฟอนต์ที่ต้องการ
                size: 14, // ขนาดของฟอนต์
              },
            },
          },
        },
      }" style="width: 200px; height: 200px" />
        </div> -->
    </div>
</template>